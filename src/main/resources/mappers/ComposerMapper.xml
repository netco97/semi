<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.wk.ComposerMapper">

<!--     <resultMap id="composerResultMap" type="ComposerDTO">
        <id property="id" column="composer_id"/>
        <result property="name" column="composer_name"/>
        <result property="genre" column="composer_genre"/>
        <result property="text" column="composer_text"/>
        <result property="img" column="composer_img"/>
        다른 필드에 대한 매핑 추가
    </resultMap> -->
    
    

    <select id="findById" resultType="ComposerDTO">
        SELECT * FROM composers WHERE composer_id = #{composer_id}
    </select>
    
    <select id="getAllArtists" resultType="ComposerDTO">
        SELECT * FROM composers
    </select>
    
    <select id="getArtistsWithPagination" resultType="ComposerDTO">
	        <![CDATA[
        SELECT *
        FROM (
            SELECT rownum AS rnum, c.*
            FROM composers c
        )
        WHERE rnum BETWEEN #{offset} AND #{offset + limit}
    ]]>
	</select>
	
	<select id="getTotalArtistsCount" resultType="int">
        SELECT COUNT(*) FROM composers
    </select>
	
    <insert id="regComposer" parameterType="ComposerDTO">
	    INSERT INTO composers (composer_id, composer_name, composer_genre, composer_text, composer_img)
	    VALUES (#{composer_id}, #{composer_name}, #{composer_genre}, #{composer_text}, #{composer_img})
	</insert>
	
	<update id="updateUserIsComposer" parameterType="String">
    UPDATE usertable SET iscomposer = 1 WHERE userFullPhoneNumber = #{userFullPhoneNumber}
	</update>
	
</mapper>